{
  "name": "wasabi-stream-worker",
  "version": "2.0.0",
  "description": "Cloudflare Worker for Wasabi video streaming with modern player UI",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "secret:set": "wrangler secret put",
    "logs": "wrangler tail",
    "test": "echo \"No tests specified\""
  },
  "keywords": ["wasabi", "cloudflare", "worker", "video", "streaming"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
